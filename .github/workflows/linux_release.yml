name: Linux Release

on:
  push:
    branches:
      - "master"
      - "develop"
    tags:
      - "v*"
  pull_request:
    branches:
      - "master"

defaults:
  run:
    shell: bash

env:
  SOURCE_DIR: ${{ github.workspace }}
  QT_VERSION: 6.10.3
  ARTIFACT: "CS3MapEdit-x86_64.AppImage"

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Copy AppIcon
        run: |
          echo "SOURCEDIR: ${{ env.SOURCE_DIR }}/"
          echo "RUNNERDIR: ${{ runner.temp }}"
          cp ${{ env.SOURCE_DIR }}/linux/CS3MapEdit-icon.png ${{ env.SOURCE_DIR }}
          echo "--- After copying icon to SOURCE_DIR root ---"
          ls -l ${{ env.SOURCE_DIR }}
          echo "--- Icon files present in repo ---"
          ls -l ${{ env.SOURCE_DIR }}/linux/CS3MapEdit-icon.png ${{ env.SOURCE_DIR }}/src/data/icons/CS3MapEdit-icon.png 2>&1

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          host: linux
          target: desktop
          arch: linux_gcc_64
          dir: ${{ runner.temp }}
          setup-python: false

      - name: Configure build with CMake
        working-directory: ${{ env.SOURCE_DIR }}
        run: |
          cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=Release -DCS3_BUILD_ZLIB_FROM_SOURCE=ON

      - name: Build
        working-directory: ${{ env.SOURCE_DIR }}
        run: |
          cmake --build build

      - name: AppImage
        working-directory: ${{ env.SOURCE_DIR }}/build
        run: |
          sudo apt update -y
          sudo apt install libxcb-cursor0
          sudo apt install libfuse2
          wget -O deploy.AppImage https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage
          chmod +x deploy.AppImage
          echo "--- SOURCE_DIR root (where icon should be) ---"
          ls -l ${{ env.SOURCE_DIR }}
          echo "--- Contents of build/ (appdir root for linuxdeployqt) ---"
          ls -l
          echo "--- Desktop file location ---"
          ls -l CS3MapEdit.desktop
          echo "--- Icon files visible from build/ ---"
          find . -maxdepth 3 -iname 'CS3MapEdit-icon*' 2>&1
          ./deploy.AppImage CS3MapEdit.desktop -appimage -no-translations -qmldir=${{ env.SOURCE_DIR }} -extra-plugins=renderers
          ls -l
          mkdir ${{ env.SOURCE_DIR }}/build/release
          ls -l
          mv CS3MapEdit*.AppImage ${{ env.SOURCE_DIR }}/build/release

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            ${{ env.SOURCE_DIR }}/build/release/*.AppImage
            LICENSE

      - name: Linux artefact
        uses: actions/upload-artifact@v5
        with:
          name: ${{ env.ARTIFACT }}
          path: ${{ env.SOURCE_DIR }}/build/release
          retention-days: 7
